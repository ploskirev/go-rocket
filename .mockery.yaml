with-expecter: true
dir: "{{.InterfaceDir}}/mocks"
filename: "mock_{{.InterfaceNameSnake}}.go"
outpkg: "mocks"
mockname: "{{.InterfaceName}}"
resolve-type-alias: False
issue-845-fix: True

packages:
  github.com/ploskirev/go-rocket/inventory/internal/service:
    config:
      include-regex: ".*Service"

  github.com/ploskirev/go-rocket/inventory/internal/repository:
    config:
      include-regex: ".*Repository"

  github.com/ploskirev/go-rocket/payment/internal/service:
    config:
      include-regex: ".*Service"

  github.com/ploskirev/go-rocket/order/internal/service:
    config:
      include-regex: ".*Service"

  github.com/ploskirev/go-rocket/order/internal/repository:
    config:
      include-regex: ".*Repository"

  github.com/ploskirev/go-rocket/order/internal/client/grpc:
    config:
      include-regex: ".*Client"